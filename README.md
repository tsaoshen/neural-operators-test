# neural-operators-test

## Burgers data generation (FNO-style, shock/front stressing)

Generate datasets:
```bash
pip install torch pyyaml
python data/generate_burgers.py --config configs/burgers.yaml
